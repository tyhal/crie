name: docker-img-npm
on:
  push:
    branches:
      - main
    tags:
      - v*
  pull_request:
    paths: ['img/pip/*']

jobs:
  build:
    uses: ./.github/workflows/docker-build-template.yml
    with:
      img: 'pip'

  push:
    uses: ./.github/workflows/docker-push-template.yml
    with:
      img: 'pip'